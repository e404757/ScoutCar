# 感知链路优化记录

- **分段计时**：增加分割推理的 `pre/input/run/output/post` 日志，并细分后处理的检测输出解析、`proto`、NMS、掩码计算和坐标还原，便于根据实测耗时定位瓶颈。
- **掩码计算**：将量化分割模型的掩码矩阵乘法放到 NPU；复用相关缓冲区，减少每帧转换开销。实测 5 个框时，`matmul` 从约 45 ms 降至约 5–8 ms，但有候选框时仍需关注掩码缩放、裁剪等耗时。
- **预处理统一**：前视图像裁掉顶部 120 行后，按比例缩放并以 114 填充到模型输入尺寸；检测框和掩码按相同的裁剪与 Letterbox 参数映回原图。移除了原先重复的 RGA 推理入口，保留一条分割推理链路。
- **零候选框快速路径**：三个检测输出均无候选框时跳过 `proto` 转换，同时保留每类原始最高分与 `Seg parse` 日志。空跑日志中的 `proto=0.00 ms`，`post` 多数约 0.6 ms；有候选框时仍需完整后处理。

构建感知包：`colcon build --symlink-install --packages-select scoutcar_perception`。
